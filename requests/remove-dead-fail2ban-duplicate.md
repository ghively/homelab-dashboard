Delete the orphaned fail2ban adapter duplicate and remove its now-dangling reference, since the real implementation is already registered elsewhere.
Where: src/adapters/security/fail2ban-adapter.ts (delete the file), src/adapters/security/index.ts (remove its import, its entry in the securityAdapters map, and its re-export)
Done means: fail2ban-adapter.ts no longer exists; index.ts no longer imports, registers, or re-exports it; the build/lint/typecheck still pass; src/lib/adapter-runtime.ts (which already uses the real src/adapters/Fail2banAdapter.ts) and src/lib/registration/security.ts are untouched.
Out of scope: src/adapters/iot-vlan-adapter.ts and src/adapters/storage/smb-nfs-adapter.ts stay as honest not-implemented stubs — do not add fabricated data or client logic to either. Do not touch src/adapters/Fail2banAdapter.ts. Do not remove the rest of src/adapters/security/index.ts (the wazuh re-exports) even though the whole barrel also appears unused elsewhere — that's a separate cleanup, not this one.
